# tfstate에는 리소스 속성이 평문으로 남는다. 커밋하지 않는다.
*.tfstate
*.tfstate.*
.terraform/
crash.log
crash.*.log

# 실제 값. *.tfvars.example만 커밋한다.
*.tfvars
!*.tfvars.example
*.auto.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json
